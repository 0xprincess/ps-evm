class DuplicationLogic {
    [void] Dup([ref] $computation, [int] $position) {
        $computation.Stack.Dup($position)
    }

    [void] Dup1([ref] $computation) {
        $this.Dup($computation, 1)
    }

    [void] Dup2([ref] $computation) {
        $this.Dup($computation, 2)
    }

    [void] Dup3([ref] $computation) {
        $this.Dup($computation, 3)
    }

    [void] Dup4([ref] $computation) {
        $this.Dup($computation, 4)
    }

    [void] Dup5([ref] $computation) {
        $this.Dup($computation, 5)
    }

    [void] Dup6([ref] $computation) {
        $this.Dup($computation, 6)
    }

    [void] Dup7([ref] $computation) {
        $this.Dup($computation, 7)
    }

    [void] Dup8([ref] $computation) {
        $this.Dup($computation, 8)
    }

    [void] Dup9([ref] $computation) {
        $this.Dup($computation, 9)
    }

    [void] Dup10([ref] $computation) {
        $this.Dup($computation, 10)
    }

    [void] Dup11([ref] $computation) {
        $this.Dup($computation, 11)
    }

    [void] Dup12([ref] $computation) {
        $this.Dup($computation, 12)
    }

    [void] Dup13([ref] $computation) {
        $this.Dup($computation, 13)
    }

    [void] Dup14([ref] $computation) {
        $this.Dup($computation, 14)
    }

    [void] Dup15([ref] $computation) {
        $this.Dup($computation, 15)
    }

    [void] Dup16([ref] $computation) {
        $this.Dup($computation, 16)
    }
}
