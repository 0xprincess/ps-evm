class SwapLogic {
    [void] Swap([ref] $computation, [int] $position) {
        $computation.Stack.Swap($position)
    }

    [void] Swap1([ref] $computation) {
        $this.Swap($computation, 1)
    }

    [void] Swap2([ref] $computation) {
        $this.Swap($computation, 2)
    }

    [void] Swap3([ref] $computation) {
        $this.Swap($computation, 3)
    }

    [void] Swap4([ref] $computation) {
        $this.Swap($computation, 4)
    }

    [void] Swap5([ref] $computation) {
        $this.Swap($computation, 5)
    }

    [void] Swap6([ref] $computation) {
        $this.Swap($computation, 6)
    }

    [void] Swap7([ref] $computation) {
        $this.Swap($computation, 7)
    }

    [void] Swap8([ref] $computation) {
        $this.Swap($computation, 8)
    }

    [void] Swap9([ref] $computation) {
        $this.Swap($computation, 9)
    }

    [void] Swap10([ref] $computation) {
        $this.Swap($computation, 10)
    }

    [void] Swap11([ref] $computation) {
        $this.Swap($computation, 11)
    }

    [void] Swap12([ref] $computation) {
        $this.Swap($computation, 12)
    }

    [void] Swap13([ref] $computation) {
        $this.Swap($computation, 13)
    }

    [void] Swap14([ref] $computation) {
        $this.Swap($computation, 14)
    }

    [void] Swap15([ref] $computation) {
        $this.Swap($computation, 15)
    }

    [void] Swap16([ref] $computation) {
        $this.Swap($computation, 16)
    }
}
